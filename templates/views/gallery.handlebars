{{#section 'head'}}
    <link rel="stylesheet" type="text/css" href="/css/gallery.css"/>
{{/section}}

<div class="container">
    <div class="row">
        {{#each images}}
            <div class="col-md-4 image-container"> 
                <img src="/images/{{path.filename}}" alt="gallery image"/>
            </div>
        {{/each}}
</div>